// const express = require('express');
// const users = require('../models/User');

// const router = express.Router();

// router.get('/dashboard', (req, res) => res.render('../views/dashboard.ejs',{ name: req.name, email: req.email, pno: req.pno, address: req.address }));

// router.get('/wishlist', (req, res) => res.render('../views/wishlist.ejs'));

// router.get('/orders', (req, res) => res.render('../views/previous.ejs'));

// module.exports = router;